[tool.poetry]
package-mode = false

[tool.poetry.dependencies]
python = "^3.10.14"
mypy = "^1.9.0"
numpy = "^1.26.4"
pygame = "^2.5.2"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
